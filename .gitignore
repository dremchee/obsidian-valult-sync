# OS / editor
.DS_Store
.env

# Local vaults used for manual testing
vaults/
.vaults/

# Plugin local environment and build artifacts
plugin/.env
plugin/node_modules/
plugin/dist/
plugin/main.js

# Server local runtime artifacts
data/
server/target/

# Accidental nested repositories created by tooling
server/.git/
